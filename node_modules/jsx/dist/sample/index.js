import { jsx, jsxs } from "react/jsx-runtime";
import { ref, watchOnly } from "../jsx.js";
import For from "../components/For.js";
const [animals, setAnimals] = ref([
  { name: "Elephant", habitat: "Savannah", isEndangered: true, count: 0 },
  { name: "Tiger", habitat: "Forest", isEndangered: true, count: 0 },
  { name: "Panda", habitat: "Bamboo Forest", isEndangered: true, count: 0 },
  { name: "Kangaroo", habitat: "Grasslands", isEndangered: false, count: 0 },
  { name: "Dolphin", habitat: "Ocean", isEndangered: true, count: 0 },
  { name: "Penguin", habitat: "Antarctica", isEndangered: false, count: 0 },
  { name: "Giraffe", habitat: "Savannah", isEndangered: false, count: 0 },
  { name: "Lion", habitat: "Savannah", isEndangered: true, count: 0 },
  { name: "Zebra", habitat: "Savannah", isEndangered: false, count: 0 },
  { name: "Koala", habitat: "Eucalyptus Forest", isEndangered: true, count: 0 }
]);
setInterval(() => {
  animals()[0].count++;
}, 2e3);
watchOnly([animals], () => console.log(animals()));
function removeAnimal(i) {
  setAnimals.byRef((animals2) => animals2.splice(i, 1));
}
function insertAnimal(i) {
  setAnimals.byRef((animals2) => animals2.splice(i + 1, 0, {
    name: "Insert",
    habitat: "NewInsert",
    isEndangered: true,
    count: 0
  }));
}
function addAnimal() {
  setAnimals.byRef((animals2) => animals2.push({
    name: "Push",
    isEndangered: true,
    count: 0,
    habitat: "NewPush"
  }));
}
const [filtered, setFiltered] = ref([...animals()]);
const [nameFilter, setNameFilter] = ref("");
watchOnly([animals], filterByName);
function filterByName() {
  if (!nameFilter()) {
    setFiltered([...animals()]);
    return;
  }
  setFiltered(animals().filter((a) => a.name.includes(nameFilter())));
}
const [index, setIndex] = ref(0);
document.head.append(/* @__PURE__ */ jsx("style", { children: style() }));
document.body.append(
  /* @__PURE__ */ jsxs("main", { children: [
    /* @__PURE__ */ jsx("input", { "on:input": (e) => {
      setNameFilter(e.currentTarget.value);
      filterByName();
    } }),
    /* @__PURE__ */ jsx(For, { each: filtered(), do: (animal, i) => /* @__PURE__ */ jsxs("div", { "class:row": true, "on:click": () => setIndex(i), children: [
      /* @__PURE__ */ jsx("strong", { children: i }),
      /* @__PURE__ */ jsxs("span", { children: [
        /* @__PURE__ */ jsxs("em", { "on:click": () => animal().count++, children: [
          animal().count,
          " ",
          animal().name,
          animal().count === 1 ? "" : "s",
          " "
        ] }),
        /* @__PURE__ */ jsxs("em", { children: [
          "in the ",
          animal().habitat,
          " ",
          animal().count === 1 ? "is" : "are",
          " "
        ] }),
        /* @__PURE__ */ jsxs("em", { children: [
          animal().isEndangered ? "" : "not",
          " endangered"
        ] })
      ] }),
      /* @__PURE__ */ jsx("button", { "on:click": () => removeAnimal(i), children: "Remove" }),
      /* @__PURE__ */ jsx("button", { "on:click": () => insertAnimal(i), children: "Add" })
    ] }) }),
    /* @__PURE__ */ jsx("button", { "on:click": addAnimal, children: "Push" }),
    /* @__PURE__ */ jsx("button", { "on:click": () => setAnimals.byRef((animals2) => animals2.sort((a, b) => a.count - b.count)), children: "Sort" }),
    /* @__PURE__ */ jsxs("h1", { children: [
      "You've clicked index #",
      index()
    ] })
  ] })
);
function style() {
  return `
    :root, body, main {
      background: #222;
      color: #fff;
      font-size: 20px;
    }

    main {
      display: grid;
      gap: 0.2em;
      padding: 1rem;
    }

    button {
      font-size: 1rem;
    }

    .row {
      background: #111A;
      border: 1px solid #AAAA;
      display: grid;
      gap: 1em;
      grid-template-columns: auto 1fr auto auto;
      align-items: center;
      padding: 0 1rem;
      user-select: none;
    }
  `;
}
//# sourceMappingURL=index.js.map
